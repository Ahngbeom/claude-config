# 로컬 설정
settings.json
settings.local.json
.credentials.json

# 플러그인 (외부 의존성)
plugins/marketplaces/
plugins/installed_plugins.json
plugins/known_marketplaces.json

# 캐시 및 히스토리
cache/
debug/
file-history/
history.jsonl
shell-snapshots/
statsig/
todos/
stats-cache.json

# 프로젝트별 설정
projects/

# 임시 파일
plans/
temp/
downloads/
*.log
image-cache/

# IDE 설정 (머신별로 다름)
ide/

# 세션 데이터
session-env/

# 플러그인 캐시 및 로컬 상태 (머신별 경로 포함)
plugins/cache/
plugins/installed_plugins.json
plugins/installed_plugins_v2.json
plugins/install-counts-cache.json

# 텔레메트리 (민감 정보 포함)
telemetry/

# 브라우저 확장
chrome/

# 붙여넣기 캐시 (민감 정보 포함 가능)
paste-cache/

# 보안 경고 상태 (로컬 세션별)
security_warnings_state_*.json

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 백업 파일 (민감 정보 포함 가능)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
*.backup*
*.bak

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 환경별 설정 파일
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
.env.*

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 에디터 임시 파일
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
*.swp
*.swo
*~

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# OS 시스템 파일
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
.DS_Store
Thumbs.db
